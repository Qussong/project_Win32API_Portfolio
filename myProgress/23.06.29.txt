
[진행 순서]
1. 	High Level Interface 아래 Input 필터 생성
2. vanInput 클래스 생성
3. vanInput.h 에 아래 멤버들 선언
		public:
			Init()
			Update()
			GetKeyDown()
			GetKeyUp()
			GetKey()
			
			GetKeyDown()
			GetKeyUp()
			GetKey()
		private:
			static std::vector<Key> mKeys;
4. vanInput.cpp에 header에 정의된 멤버들 정의
5. vanApplication.cpp
	- Application::Init()에 Input::Init() 추가
	- Application::Update() 키 입력 받기 수정 (ver1 -> ver2)