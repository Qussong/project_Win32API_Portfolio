
[ 오늘의 작업 ]
- Rigid Body 만들기

---

[ Rigid Body ]

function :
	Init()
	Update()
	Render(HDC hdc)
	
	void AddForce(Vecotr2 force) { mForce += force; }
	void SetMass(float mass) { mMass = mass; }

variable :
	float mMass;
	
	float mFriction;			// 마찰력
	//float mStaticFriction;	// 정지 마찰력
	//float mKineticFriction;	// 운동 마찰력
	float mCoefficentFriction;	// 물체마다 고유한 마찰이 다르기에 그 값을 조정해주는 역할
	
	Vector2 mForce;
	Vector2 mAccelation;
	Vector2 mVelocity;
	Vector2 mGravity;
	

- Vector2 에 Nomarlize() 함수 생성	-> 대각선 이동 때문에
- Vector2 에 Length() 함수 생성

---

월요일에 점프 만든다.