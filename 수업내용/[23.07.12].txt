
[ 오늘의 작업 ]
- Camera 필터 생성 Camera 클래스 생성
	여기저기서 사용하기때문에 static으로 생성

	class Camera
	{
	public:
		static void Init();
		static void Update();
		
		static GameObject* GetTarget() {return mTarget; }
		static void SetTarget(GameObject* _target) { mTarget = _target; }
		
	private:
		static Vector2 mResolution;	// 해상도
		static Vector2 mLookPosition;
		static Vector2 mDistatnce;
		static Vector2 mTarget;
		
	}

- vanMath.h 수정 :operater override

- vanSpriteRenderer 수정

- 

---

[ NDC 좌표계 ]
- 화면의 가운대가 (0,0) 인 좌표계

---

[ 알파 블랜딩 ]

---