[23/06/26]

---[ 포폴 ] ---

- 포폴 구성
	타이틀 씬 -> 튜토리얼 -> 컨텐츠
- 씬 개수가 포폴의 장점이 된다.
- API : 첫달(공부기간) -> 두번째달(포폴 만들기)
	첫달에 배경작업, 타이틀 작업 해두기
- 툴에 너무 시간을 할애하지 마라 우린 게임 프로그래머다
	툴 프로그래머가 아니다.

---[ 1교시 ]---

[WCHAR ?]
- 	char엔 단점이 있다. 
	char 타입엔 영어의 아파벳이 전부 들어갈 수 있다.

-	한글의 조합 수는 1만 1,172 가지
	때문에 char 타입에 전부 넣을 수 없다.
	그렇기에 WCHAR 타입을 사용한다.
-	WHCAR 는 2Byte
-	초기화 할때 앞에 'L' 을 넣어준다.
	ex)
		wchar_t ch = L'a';

[ 제목표시줄 문자열 ]
- 리소스뷰 -> project명 클릭 -> String Table -> 확인
- 테이블에 있는 값 수정 가능하다

[ CreateWindowW() ]

- WS_OVERLAPPERWINDOW : 윈도우 창 스타일 중 하나
- 대부분의 API들은 직접적으로 접근해서 값을 변경 할 수 있는 권한을 주지 않는다.
	(잘못된 값이 들어가도 문제가 되지 않도록 하기 위해서...)
- HWND 라는 반환값을 내놓는다. 
- 포인터 값이다.

[ ShowWindow() ]
- 화면에 띄우려고 하는 윈도우의 주소값이 인자로 들어간다.

[ UpdateWindow() ]

[ MyRegisterClass() ]
- 윈도우 관련 여러 옵션들이 설정된다.

[ szWindowClass ]
- wcex.lpszClassName  = szWindowClass;
- 윈도우의 클래스 이름을 가지고 있는 변수
- CreateWinodwW()로 윈도우창을 만들때 해당

[ MSG ]
- 메세지 큐에 메세지가 들어가고 해당 메세지를 처리하는 방식으로 Windows가 구동된다.
- 

[ __stdcall ]
- 함수규약
- win32 함수를 호출하는 규약

[ 함수 포인터 ]
int sum(int a, int b)
{
	return a + b;
}

void* p = &sum;	// void 형엔 다 받을 수 있다.

int (*ptrSum)(int a, int b);	// 함수 포인터 변수 선언 법
ptrSum = sum;

ptrSum(1,2);	// 3

[ 기본구조 ]
- 메세지 응답 구조이다보니 메세지가 있을때만 작동한다.
- 게임에는 적합하지 않은 구조이다.
- 우리는 GetMessage 가 아닌 PeekMessage를 사용한다.

[ HDC ]
- 윈도우 안에 그리는 작업을 도와주는 주소?

[ 오늘 숙제 ]
- 도형 그려보기
- 한줄 씩 그려보기
- gitignore 활용해서 파일 제외시켜서 git에 commit 하기
